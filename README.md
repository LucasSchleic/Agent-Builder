# Agent-Builder
Agent Builder is a lightweight, fully local web application for visually composing AI agent workflows. Build pipelines by connecting LLM, Agent, HTTP, and Python Script blocks on a canvas, then export them as standalone Python scripts.
